.PHONY: tests coverage

tests:
	nosetests tests

coverage:
	nosetests --with-coverage --cover-branches --cover-package=shellypython tests
	coverage html --include='shellypython*'

#release:
#python setup.py sdist bdist_wheel upload